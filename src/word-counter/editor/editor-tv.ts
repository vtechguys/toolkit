import { tv } from "tailwind-variants";

export const editorTV = tv({
  base: "grow outline-none",
});

export const editorWrapperTV = tv({
  base: "rounded-2xl m-4 w-10/12 bg-white lg:min-h-96 flex border border-slate-200 outline-none shadow-slate-400",
});
